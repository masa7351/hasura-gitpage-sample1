overwrite: true
schema: "http://localhost:8080/v1/graphql"
documents: "queries/**/*.ts"
generates:
  types/graphqlSchema.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-resolvers"
      - "typescript-react-apollo"
